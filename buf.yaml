version: v2
modules:
  - path: .
    excludes:
      - '@byo-ai-agent-platform'
      - computer_controller
      - local_models


deps:
  - buf.build/bufbuild/protovalidate
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE


version: v2
modules:
  - path: .
    excludes:
      - packages
      - apps
      - local_models


deps:
  - buf.build/bufbuild/protovalidate
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE

